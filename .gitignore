.pio/
.pio-core/
.vscode/
logs/
src/config/Secrets.h
*.ttf
# Agent-tooling scratch space (cloned repos, node_modules, skill installs) --
# not project source, must never be committed.
.agents/tools/
skills-lock.json
.bak*
# Generated Doxygen output -- built on demand via scripts/build-docs.ps1,
# never committed (see docs/release.md).
doxygen-output/
doxygen-warnings.log
debug/
# Per-release firmware.elf archive (scripts/release.ps1) -- attached only
# as a GitHub-Release asset for later coredump analysis, never committed
# (an ELF with debug info is far larger than firmware.bin and would
# needlessly bloat the repository).
firmware.elf.zip
firmware.elf.zip.sha256